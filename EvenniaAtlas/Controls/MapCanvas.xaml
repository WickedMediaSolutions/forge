<UserControl x:Class="EvenniaAtlas.Controls.MapCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E1E"
             Focusable="True"
             KeyDown="OnKeyDown">
    <Grid>
        <ScrollViewer x:Name="ScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="#1E1E1E"
                      ScrollChanged="OnScrollChanged">
            <Canvas x:Name="MapSurface"
                    Width="14000" Height="14000"
                    Background="#252526"
                    MouseLeftButtonDown="OnMapMouseDown"
                    MouseRightButtonDown="OnMapRightClick"
                    MouseMove="OnMapMouseMove"
                    MouseLeftButtonUp="OnMapMouseUp"
                    MouseWheel="OnMapWheel">
                <Canvas.LayoutTransform>
                    <ScaleTransform ScaleX="{Binding ZoomScale}"
                                    ScaleY="{Binding ZoomScale}"/>
                </Canvas.LayoutTransform>
                <Canvas x:Name="GridLayer" />
                <Canvas x:Name="ConnectionsLayer" />
                <Canvas x:Name="RoomsLayer" />
            </Canvas>
        </ScrollViewer>
    </Grid>
</UserControl>
