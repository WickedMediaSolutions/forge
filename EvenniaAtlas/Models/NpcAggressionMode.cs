namespace EvenniaAtlas.Models;

/// <summary>Data classification for NPC aggression posture.
/// These values represent configuration intent only — no runtime behavior is defined here.</summary>
public enum NpcAggressionMode
{
    Passive,
    Defensive,
    Aggressive
}