<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Evennia green accent LED colors – restrained, professional -->
    <SolidColorBrush x:Key="LedOnGreen" Color="#4CAF50"/>
    <SolidColorBrush x:Key="LedOffGray" Color="#555555"/>

    <!-- Dark theme colors -->
    <SolidColorBrush x:Key="BackgroundDark" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="BackgroundMedium" Color="#2D2D2D"/>
    <SolidColorBrush x:Key="BackgroundLight" Color="#3C3C3C"/>
    <SolidColorBrush x:Key="BorderColor" Color="#555555"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="TextMuted" Color="#999999"/>
    <SolidColorBrush x:Key="AccentBlue" Color="#007ACC"/>
    <SolidColorBrush x:Key="AccentGold" Color="#FFD700"/>

    <!-- Button style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="#3C3C3C"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#555555"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#4C4C4C"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#505050"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox style -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#3C3C3C"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#555555"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="CaretBrush" Value="White"/>
    </Style>

    <!-- ComboBox style -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#3C3C3C"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#555555"/>
        <Setter Property="Padding" Value="6,4"/>
    </Style>

    <!-- CheckBox style -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#444444"/>
    </Style>

    <!-- ToolBarTray -->
    <Style TargetType="ToolBarTray">
        <Setter Property="Background" Value="#2D2D2D"/>
    </Style>

    <!-- Separator -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="#555555"/>
        <Setter Property="Height" Value="1"/>
    </Style>


    <!-- Evennia green accent -->
    <SolidColorBrush x:Key="EvenniaGreen" Color="#4CAF50"/>

    <!-- Shell colors – professional dark palette -->
    <SolidColorBrush x:Key="ShellHeaderBg" Color="#1D1F21"/>
    <SolidColorBrush x:Key="ShellToolbarBg" Color="#232529"/>
    <SolidColorBrush x:Key="ShellToolbarBorder" Color="#35383D"/>
    <SolidColorBrush x:Key="ShellSeparatorBrush" Color="#35383D"/>
    <SolidColorBrush x:Key="ShellGroupBg" Color="#2A2D31"/>
    <SolidColorBrush x:Key="ShellMenuBg" Color="#232529"/>
    <SolidColorBrush x:Key="ShellMenuText" Color="#E2E2E2"/>
    <SolidColorBrush x:Key="ShellMenuHoverBg" Color="#303238"/>
    <SolidColorBrush x:Key="ShellMenuPressedBg" Color="#25282D"/>
    <SolidColorBrush x:Key="ShellMenuBorder" Color="#35383D"/>

    <!-- Toolbar separator (vertical line) -->
    <Style x:Key="ToolbarSeparatorStyle" TargetType="Rectangle">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Fill" Value="{StaticResource ShellSeparatorBrush}"/>
        <Setter Property="Margin" Value="3,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Toolbar button — low-profile, restrained -->
    <Style x:Key="ToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#D0D0D0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#303238"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#40444A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#25282D"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#35383D"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#62656A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu bar -->
    <Style x:Key="ShellMenuStyle" TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource ShellMenuBg}"/>
        <Setter Property="Foreground" Value="{StaticResource ShellMenuText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ShellToolbarBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Padding" Value="2,0"/>
    </Style>

    <!-- Top-level MenuItem -->
    <Style x:Key="ShellTopMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ShellMenuText}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ShellMenuHoverBg}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#62656A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu Item (dropdown) -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="#2A2D31"/>
        <Setter Property="Foreground" Value="#D0D0D0"/>
        <Setter Property="Padding" Value="24,4,12,4"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="itemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                                  Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding InputGestureText}"
                                           Foreground="#9A9DA3" FontSize="10.5"
                                           Margin="40,0,0,0" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Popup x:Name="SubMenuPopup" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               PopupAnimation="Fade" Placement="Right"
                               HorizontalOffset="-2" VerticalOffset="-3" AllowsTransparency="True">
                            <Border Background="#2A2D31" BorderBrush="#40444A" BorderThickness="1" CornerRadius="2" Padding="0,2">
                                <StackPanel IsItemsHost="True"/>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="#303238"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#62656A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu Separator -->
    <Style x:Key="ShellMenuSeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="#40444A"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="12,3,12,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"
                            Margin="{TemplateBinding Margin}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>